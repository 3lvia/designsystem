Elvia component is an HTMLElement (web component) which should can be extended to all web components.

The component is taking care of:

- wrapping the react component
- change handling
- data-binding
- attaching style to web component
